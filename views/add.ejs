<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        @import url("https://fonts.googleapis.com/css2?family=Sansita+Swashed:wght@600&display=swap");

body {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background: #f0f0f0; /* Soft neutral background for simplicity */
    font-family: "Sansita Swashed", cursive;
}

.center {
    padding: 40px;
    background: #ffffff;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    text-align: center;
    width: 100%;
    max-width: 400px;
}

.center h1 {
    font-size: 1.8em;
    color: #333;
    margin-bottom: 30px;
    border-left: 4px solid #0077B6;
    padding-left: 10px;
}

.inputbox {
    position: relative;
    margin-bottom: 20px;
}

.inputbox input,
.inputbox button {
    width: 100%;
    padding: 12px;
    font-size: 1em;
    border: 1px solid #ccc;
    border-radius: 5px;
    outline: none;
    box-sizing: border-box;
}

.inputbox input:focus,
.inputbox button:hover {
    border-color: #0077B6;
    transition: border-color 0.3s ease;
}

.inputbox span {
    position: absolute;
    left: 15px;
    top: 12px;
    font-size: 0.9em;
    color: #888;
    pointer-events: none;
    transition: 0.3s;
}

.inputbox input:focus ~ span,
.inputbox input:valid ~ span {
    top: -10px;
    left: 10px;
    font-size: 0.8em;
    color: #0077B6;
}

.inputbox button {
    background: #0077B6;
    color: #fff;
    border: none;
    cursor: pointer;
}

.inputbox button:hover {
    background: #005f8d;
}

    </style>
</head>
<body>
    
    <div class="center">
        <h1>Create Blog</h1>
        <form class="item" action="/submit" method="post" enctype="multipart/form-data">
          <div class="inputbox" >
            <input type="text" required="required" name="head">
            <span>Heading</span>
          </div>
          <div class="inputbox" >
            <input type="text" required="required" name="desci">
            <span>Description</span>
          </div>
          <div class="inputbox" >
            <input type="file" name="image" accept="image/*"  required="required">
           
          </div>
          
          <div class="inputbox">
            <button class="submit" type="submit" name="list" ><h3>Submit</h3></button>
          </div>
        </form>
      </div>
</body>
</html>



